# Core libraries for data manipulation and processing
pandas==1.5.3       # For data manipulation and analysis
numpy==1.24.2       # For numerical computations and array handling

# Libraries for NLP preprocessing and text manipulation
nltk==3.8.1         # For text tokenization, stopword removal, and stemming/lemmatization
spacy==3.5.1        # For advanced NLP tasks like named entity recognition and lemmatization
gensim==4.3.1       # For topic modeling (LDA) and Word2Vec embeddings

# Libraries for machine learning and deep learning
scikit-learn==1.2.1 # For classical machine learning models and evaluation metrics
torch==2.0.0        # PyTorch: For fine-tuning transformer-based models
transformers==4.26.1 # Hugging Face Transformers library for using models like BERT and DistilBERT
tensorflow==2.12.0  # For training deep learning models if required

# Libraries for visualization
matplotlib==3.7.1   # For creating basic plots and visualizations
seaborn==0.12.2     # For statistical data visualization (e.g., confusion matrices)
wordcloud==1.8.2    # For creating word cloud visualizations
plotly==5.14.1      # For interactive topic modeling visualizations (e.g., t-SNE, UMAP)

# Libraries for explainability and interpretability
lime==0.2.0.1       # For generating explanations for model predictions
shap==0.41.0        # For SHapley Additive exPlanations

# Libraries for dimensionality reduction and clustering in topic modeling
umap-learn==0.5.3   # For UMAP visualizations in topic modeling
hdbscan==0.8.29     # For clustering topics in BERTopic

# BERTopic: Advanced topic modeling library
bertopic==0.13.0    # For transformer-based topic modeling

# Miscellaneous
jupyterlab==3.6.2   # For running and managing Jupyter notebooks
